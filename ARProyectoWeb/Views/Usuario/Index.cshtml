@model List<ARProyectoWeb.Data.Models.Usuario>

@{
    ViewData["Title"] = "Usuarios";
}
@Html.ActionLink("Crear Usuario","Create",new{@class="btn-primary"})

<br />
<br />

@if (Model.Count > 0)
{
    <div class="table-responsive">
        <table class="table">
            <thead class="table-dark">
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Correo</th>
                    <th>Fecha de nacimiento</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="align-middle">
                        <td>
                            @item.UsuarioId
                        </td>
                        <td>
                            @item.Nombre
                        </td>
                        <td>
                            @item.Correo
                        </td>
                        <td>
                            @item.FechaNacimiento.Date
                        </td>
                        <td>
                            @Html.ActionLink("Editar","Edit",new{id=item.UsuarioId},new{@class="btn btn-primary"})
                            @Html.ActionLink("Eliminar","Delete",new{id=item.UsuarioId},new{@class="btn btn-alert"})
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
